# PDF EXE 静态提取工具 1.1

必须先完整解压 ZIP，不能直接在压缩包窗口中运行 BAT。

- 第一次运行：`install_and_run.bat`
- 日常运行：`run_tool.bat`
- 故障诊断：`diagnose.bat`
- 打包独立程序：`build_windows_exe.bat`

本版启动器不隐藏 Python 错误；失败时会保留命令窗口，并把信息写入 `startup_error.log` 或 `diagnostic_log.txt`。
